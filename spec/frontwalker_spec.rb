require 'spec_helper'

describe Frontwalker do
  it 'has a version number' do
    expect(Frontwalker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
